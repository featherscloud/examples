{
  "name": "examples",
  "version": "1.0.0",
  "description": "Examples to get started with Feathers Cloud Auth",
  "main": "index.js",
  "workspaces": [
    "server/*",
    "client/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-dependencies": "npm exec --workspaces --include-workspace-root -- ncu -u"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-check-updates": "^17.0.0"
  }
}